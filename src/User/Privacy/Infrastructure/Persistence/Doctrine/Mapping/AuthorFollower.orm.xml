<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!--
        The follower graph, projected from `Community` (`FEAT-COM-010`).

        The pair is the primary key: reprocessing the same fact writes the
        same row. No foreign keys — this is a copy of somebody else's facts,
        and a copy that constrains its source is a copy that blocks it.
    -->
    <entity name="LectoresBeta\User\Privacy\Domain\Entity\AuthorFollower"
            table="author_follower" schema="user_ctx">

        <!-- La dirección que no cubre la clave primaria: a quién sigue alguien. -->
        <indexes>
            <index name="idx_author_follower_follower" columns="follower_id"/>
        </indexes>

        <id name="authorId" column="author_id" type="guid">
            <generator strategy="NONE"/>
        </id>

        <id name="followerId" column="follower_id" type="guid">
            <generator strategy="NONE"/>
        </id>

        <field name="followedAt" column="followed_at" type="datetime_immutable"/>

    </entity>

</doctrine-mapping>
