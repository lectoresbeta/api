<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!--
        Qué propuestas admite cada persona (`FEAT-USR-011`).

        Una fila por usuario, y solo cuando la toca: la ausencia se lee como
        los valores por defecto, que son los dos abiertos.
    -->
    <entity name="LectoresBeta\User\Preferences\Domain\Entity\UserReceptionSettings"
            table="user_reception_settings" schema="user_ctx">

        <id name="userId" column="user_id" type="guid">
            <generator strategy="NONE"/>
        </id>

        <field name="betaReaderInvitations" column="beta_reader_invitations" type="boolean"/>
        <field name="writingBuddyProposals" column="writing_buddy_proposals" type="boolean"/>
        <field name="updatedAt" column="updated_at" type="datetime_immutable"/>

    </entity>

</doctrine-mapping>
