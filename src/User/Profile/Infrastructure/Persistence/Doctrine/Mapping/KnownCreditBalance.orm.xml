<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!-- La copia del saldo que `User` mantiene para pintar el menú lateral.
         Una fila por cuenta: no es un historial, es el último número que se
         sabe. El historial es de `Credits` y no sale de ahí. -->
    <entity name="LectoresBeta\User\Profile\Domain\Entity\KnownCreditBalance"
            table="known_credit_balance" schema="user_ctx">

        <id name="userId" type="guid" column="user_id">
            <generator strategy="NONE"/>
        </id>

        <field name="balance" type="integer"/>
        <field name="knownAt" column="known_at" type="datetime_immutable"/>

    </entity>

</doctrine-mapping>
