<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!-- The table keeps the name the specification uses, `user_genre`. -->
    <entity name="LectoresBeta\User\Profile\Domain\Entity\LiteraryPreference"
            table="user_genre" schema="user_ctx">

        <indexes>
            <!-- The reverse direction is the one the author suggestions need
                 (`FEAT-COM-016`): who is interested in this genre. -->
            <index name="idx_user_genre_genre" columns="genre_code"/>
        </indexes>

        <id name="userId" column="user_id" type="guid">
            <generator strategy="NONE"/>
        </id>
        <id name="genreCode" column="genre_code" type="string" length="32">
            <generator strategy="NONE"/>
        </id>

        <field name="selectedAt" column="selected_at" type="datetime_immutable"/>

    </entity>

</doctrine-mapping>
