<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!-- Composite key (user, tour): there is nothing else to identify a row
         by, and a surrogate id would allow the same tour twice. -->
    <entity name="LectoresBeta\User\Onboarding\Domain\Entity\UserTour"
            table="user_tour" schema="user_ctx">

        <id name="userId" column="user_id" type="guid">
            <generator strategy="NONE"/>
        </id>
        <id name="tourId" column="tour_id" type="string" length="64">
            <generator strategy="NONE"/>
        </id>

        <field name="lastStep" column="last_step" type="integer" nullable="true"/>
        <field name="dismissed" type="boolean"/>
        <field name="completedAt" column="completed_at" type="datetime_immutable" nullable="true"/>
        <field name="updatedAt" column="updated_at" type="datetime_immutable"/>

    </entity>

</doctrine-mapping>
