<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="LectoresBeta\Reading\AccessRequest\Domain\Entity\AccessRequest"
            table="access_request" schema="reading_ctx">

        <indexes>
            <!-- The author's queue of pending requests. -->
            <index name="idx_access_request_author" columns="author_id,status"/>
            <index name="idx_access_request_requester" columns="requester_id,status"/>
            <index name="idx_access_request_work" columns="work_id,status"/>
        </indexes>

        <unique-constraints>
            <!-- One pending request per (reader, work). Asking again after a
                 rejection has to stay possible, hence the WHERE. -->
            <unique-constraint name="uniq_access_request_pending" columns="work_id,requester_id">
                <options>
                    <option name="where">((status)::text = 'PENDING'::text)</option>
                </options>
            </unique-constraint>
        </unique-constraints>

        <id name="id" type="guid" column="id">
            <generator strategy="NONE"/>
        </id>

        <field name="workId" column="work_id" type="guid"/>
        <field name="requesterId" column="requester_id" type="guid"/>
        <field name="authorId" column="author_id" type="guid"/>
        <field name="status" type="string" length="16"
               enum-type="LectoresBeta\Reading\AccessRequest\Domain\Enum\AccessRequestStatus"/>
        <field name="message" type="text" nullable="true"/>
        <field name="requestedAt" column="requested_at" type="datetime_immutable"/>
        <field name="resolvedAt" column="resolved_at" type="datetime_immutable" nullable="true"/>

    </entity>

</doctrine-mapping>
