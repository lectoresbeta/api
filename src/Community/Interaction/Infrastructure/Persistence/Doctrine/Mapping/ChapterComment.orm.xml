<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!--
        Comentarios bajo el texto de un capítulo (`FEAT-COM-036`).

        Tabla aparte de `post_comment` y no un `post_id` anulable: heredan
        audiencias distintas —una publicación frente a la regla de lectura de
        una obra— y dos caminos de autorización en una entidad es la forma
        más rápida de aplicar un día el que no toca.

        Sin clave ajena a `chapter_id`: es de `Work`, y una copia que
        restringe a su origen es una copia que lo bloquea.
    -->
    <entity name="LectoresBeta\Community\Interaction\Domain\Entity\ChapterComment"
            table="chapter_comment" schema="community_ctx">

        <indexes>
            <index name="idx_chapter_comment_chapter" columns="chapter_id,created_at"/>
            <index name="idx_chapter_comment_parent" columns="parent_comment_id"/>
        </indexes>

        <id name="id" type="guid" column="id">
            <generator strategy="NONE"/>
        </id>

        <field name="chapterId" column="chapter_id" type="guid"/>
        <field name="authorId" column="author_id" type="guid"/>
        <field name="parentCommentId" column="parent_comment_id" type="guid" nullable="true"/>
        <field name="body" type="text"/>
        <field name="replyCount" column="reply_count" type="integer"/>
        <field name="createdAt" column="created_at" type="datetime_immutable"/>
        <field name="deletedAt" column="deleted_at" type="datetime_immutable" nullable="true"/>

    </entity>

</doctrine-mapping>
