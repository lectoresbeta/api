<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!-- `(work_id, version)` is unique: it both finds the current version and
         resolves the one a given correction answered (`FEAT-WRK-014`). -->
    <entity name="LectoresBeta\Work\Questionnaire\Domain\Entity\Questionnaire"
            table="questionnaire" schema="work_ctx">

        <unique-constraints>
            <unique-constraint name="uniq_questionnaire_version" columns="work_id,version"/>
        </unique-constraints>

        <id name="id" type="guid" column="id">
            <generator strategy="NONE"/>
        </id>

        <field name="workId" column="work_id" type="guid"/>
        <field name="version" type="smallint"/>
        <field name="createdAt" column="created_at" type="datetime_immutable"/>
        <field name="updatedAt" column="updated_at" type="datetime_immutable"/>

    </entity>

</doctrine-mapping>
