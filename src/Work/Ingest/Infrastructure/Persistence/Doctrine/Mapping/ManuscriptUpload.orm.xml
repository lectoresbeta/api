<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!--
        Un manuscrito subido y todavía sin confirmar (`FEAT-WRK-002`).

        Es una tabla de paso: vive horas, se borra al confirmar y la purga se
        lleva lo que nadie confirmó. El índice va por caducidad, que es como
        la recorre esa purga.

        El fichero original **no se guarda** en ninguna parte.
    -->
    <entity name="LectoresBeta\Work\Ingest\Domain\Entity\ManuscriptUpload"
            table="manuscript_upload" schema="work_ctx">

        <indexes>
            <index name="idx_manuscript_upload_expiry" columns="expires_at"/>
        </indexes>

        <id name="id" type="guid" column="id">
            <generator strategy="NONE"/>
        </id>

        <field name="authorId" column="author_id" type="guid"/>
        <field name="filename" type="string" length="120"/>
        <field name="chapters" type="json">
            <options>
                <option name="jsonb">true</option>
            </options>
        </field>
        <field name="uploadedAt" column="uploaded_at" type="datetime_immutable"/>
        <field name="expiresAt" column="expires_at" type="datetime_immutable"/>

    </entity>

</doctrine-mapping>
