<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!-- La clave es el invitado: a cada persona la trae alguien una sola vez
         y para siempre (`FEAT-CRD-005` `RN-2`). Es la base de datos quien lo
         garantiza, no el código: dos hechos `PlatformInvitationConsumed` para
         la misma persona no pueden crear dos filas. -->
    <entity name="LectoresBeta\Credits\Referral\Domain\Entity\Referral"
            table="referral" schema="credits_ctx">

        <indexes>
            <!-- El tope de diez recompensas por invitador se cuenta por aquí
                 en cada corrección de un invitado (`RN-5`). -->
            <index name="idx_referral_inviter" columns="inviter_id,rewarded_at"/>
        </indexes>

        <id name="inviteeId" column="invitee_id" type="guid">
            <generator strategy="NONE"/>
        </id>

        <field name="inviterId" column="inviter_id" type="guid"/>
        <field name="linkedAt" column="linked_at" type="datetime_immutable"/>
        <field name="rewardedAt" column="rewarded_at" type="datetime_immutable" nullable="true"/>

    </entity>

</doctrine-mapping>
