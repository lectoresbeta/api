<?xml version="1.0" encoding="utf-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="LectoresBeta\Credits\Overdraft\Domain\Entity\OverdraftGrant"
            table="overdraft_grant" schema="credits_ctx">

        <indexes>
            <!-- Counting the quota of a week is an equality on this pair. -->
            <index name="idx_overdraft_quota" columns="quota_period,author_id"/>
            <index name="idx_overdraft_unsettled" columns="settled_at"/>
        </indexes>

        <id name="id" type="guid" column="id">
            <generator strategy="NONE"/>
        </id>

        <field name="authorId" column="author_id" type="guid"/>
        <field name="chapterId" column="chapter_id" type="guid"/>
        <field name="quotaPeriod" column="quota_period" type="string" length="8"/>
        <field name="amount" type="smallint"/>
        <field name="grantedAt" column="granted_at" type="datetime_immutable"/>
        <field name="settledAt" column="settled_at" type="datetime_immutable" nullable="true"/>

    </entity>

</doctrine-mapping>
