# Endpoints — `<BoundedContext>`

> Convenciones transversales en [`../api/conventions/`](../api/conventions/). Esquemas en `openapi/`.
> (Al copiar esta plantilla a `docs/api/endpoints/`, el enlace pasa a ser `../conventions/`.)

## Resumen

| Método y ruta | `operationId` | Propósito | Funcionalidad | Estado |
|---|---|---|---|---|

---

## `<MÉTODO> /<ruta>`

**`operationId`:** `<id>` · **Funcionalidad:** `FEAT-XXX-000`

### Propósito

Una frase.

### Autorización

Quién puede llamarla y bajo qué condición.

### Reglas aplicadas

Referencia a las `RN-n` de la ficha de la funcionalidad. No se reescriben aquí.

### Entrada

Qué representa cada campo relevante. El esquema está en `openapi/`.

### Respuesta

Qué devuelve y qué significa. Código de éxito.

### Errores específicos

| `code` | HTTP | Cuándo |
|---|---|---|

### Efectos

Eventos publicados, movimientos de créditos, notificaciones.
